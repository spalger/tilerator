vector:
  public: false
  formats: [pbf]
  uri: bridge://
  xml:
    npm: ["osm-bright-source", "data.xml"]
  xmlSetDataSource:
    if:
      dbname: gis
      host: ''
      type: postgis
    set:
      host: {var: osmdb-host}
      port: {var: osmdb-port}
      user: {var: osmdb-user}
      password: {var: osmdb-pass}
      dbname: {var: osmdb-dbname}

cass:
  public: false
  formats: [pbf]
  uri: cassandra://
  params:
    keyspace: v1
    cp: [localhost]
    createIfMissing: true

getpbf:
  public: false
  formats: [pbf]
  uri: autogen://
  params:
    storage: {ref: cass}
    generator: {ref: vector}
    mingen: 0
    maxgen: 12
    minstore: 0
    maxstore: 12

default:
  public: true
  formats: [png]
  scales: []
  uri: vector://
  xml:
    loader: osm-bright-style
  xmlSetParams:
    source: {ref: vector}
